<h2><%= l(:label_repository)%></h2>
<%# This can be used to display basic git setup instructions, like on github... %>
<% html_title(l(:label_repository)) -%>
<a href='<%= "guthub-mac://openRepo/#{l(:label_repository)}"  -%>'>Open in mac</a>
